[project]
name = "guided-meditation"
version = "0.2.0"
description = "Generate guided meditation audio for a given number of minutes using Kokoro TTS"
requires-python = ">=3.10"
dependencies = [
    "kokoro>=0.9.2",
    "soundfile==0.12.1",
    "numpy>=1.24",
]
